# AI 识别
openai-whisper>=20231117        # 本地 Whisper（离线备选）
groq>=0.9.0                     # Groq API 客户端（默认在线方案）

# 音频处理
pydub>=0.25.1
mutagen>=1.47.0

# 人声分离（可选）
demucs>=4.0.1

# 桌面 GUI
PyQt6>=6.6.0

# 文件同步
aiohttp>=3.9.0
zeroconf>=0.131.0               # mDNS 设备发现

# 工具
ffmpeg（系统安装，非 pip）
