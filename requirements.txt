# Echovault 基础运行依赖
#
# ffmpeg 是系统程序，不是 Python 包。安装说明见 README.md。
# 云端和本地 ASR 引擎分别放在 requirements-cloud.txt 与
# requirements-local.txt，避免基础安装自动拉取大型 AI 依赖。

# 桌面 GUI
PyQt6>=6.6.0,<7

# 音频与元数据
pydub>=0.25.1,<1
mutagen>=1.47.0,<2
opencc-python-reimplemented>=0.1.7,<1

# 讯飞极速录音转写与流式语音听写 Provider 会随基础桌面程序加载
requests>=2.31.0,<3
websocket-client>=1.8.0,<2

# 文件同步与 LocalSend
aiohttp>=3.9.0,<4
zeroconf>=0.131.0,<1
cryptography>=42.0.0,<49
psutil>=5.9.0,<8
